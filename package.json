{
  "name": "the-wire",
  "version": "1.0.0",
  "private": true,
  "description": "The Wire. A personal daily news briefing (6 desks) generated by Claude + web search, on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "refresh": "echo 'POST /api/refresh on your deployed URL to force a regenerate'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

